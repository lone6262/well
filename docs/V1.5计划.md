# 宠物症状自查小程序 V1.5 — 完整开发计划与进度总览

## 项目概况

| 项目 | 说明 |
|---|---|
| **产品** | 宠物健康风险评估 + AI健康报告 + 会员服务平台 |
| **技术栈** | 微信小程序原生 + 微信云开发（云函数 + 云数据库） |
| **AppID** | `wxee33a0c47421db2c` |
| **云环境** | `cloud1-d8gdi44zqfec250b5` |
| **开发排期** | 8周计划（已压缩为持续推进） |
| **完善版 PRD** | `docs/V1.5商业化增强版PRD_完善版.md` |

---

## ✅ Phase 1：基础安全修复 + 建库 + 知识库 + 报告模板（已完成）

### Step 1: openid 安全漏洞修复（8+1个云函数）
- `submitSymptom`, `getPetList`, `getRecordList`, `getRecordDetail`, `getUserStats`, `saveUserProfile`, `deletePet`, `deleteRecord`, `savePet`
- 全部改用 `cloud.getWXContext().OPENID` 服务端获取
- 前端 17 处 openid 传参全部移除
- `miniprogram/utils/api.js` 移除自动注入 openid 逻辑

### Step 2: Token 验证机制
- 新建 `cloudfunctions/common/auth.js`（getOpenid + verifyToken + authenticate）
- `deletePet`, `deleteRecord` 集成 Token 验证
- 前端删除操作传入 `app.globalData.token`

### Step 3: 敏感日志清理
- 9 个文件 15+ 处日志脱敏（云函数 + 前端）
- `searchHospitals/package.json` SDK 版本固定为 `~2.6.3`

### Step 4: 技术债清理
- `scripts/sync-constants.sh` 同步脚本创建
- 测试页面待删除（cloud-test/, login-test/, test-login/）

### Step 5: constants.js V1.5 扩展
- 新增 15 组常量：MEMBER_STATUS, REPORT_SOURCE, CACHE_TTL, ARTICLE_STATUS, FOLLOWUP_STATUS, PRICES（分）, MEMBER_CREDITS, PAYMENT_TIMEOUT, KNOWLEDGE_CATEGORIES, AGE_RANGES
- 同步到 16+ 云函数目录

### Step 6: 数据库建表 + 初始数据
- 扩展 `dbInit/index.js`：5 个新集合（members, knowledge_articles, invite_records, followup_records, report_templates）
- 8 篇科普知识文章初始数据
- 6 个报告模板（低/中/高 × 2 变体）

### Step 7: 知识库功能
- 云函数：`getKnowledgeList`, `getKnowledgeDetail`
- 页面：`pages/knowledge/list.*`（分类筛选 + 分页）、`pages/knowledge/detail.*`（富文本 + 分享）
- 首页 `toKnowledge()` 改为 `wx.navigateTo`
- app.json 注册 2 个新页面

### Step 8: 报告模板引擎
- `cloudfunctions/common/report-engine.js`：generateCacheKey, getAgeRange, selectTemplate, fillTemplate, generateReport
- `cloudfunctions/generateAIReport/`：完整报告生成流程（缓存 → 模板 → 填充 → 存储 → 创建回访记录）

### Step 9: 会员状态查询骨架
- `cloudfunctions/getMemberStatus/`：查询会员状态 + 额度检查 + 过期处理
- `cloudfunctions/checkReportQuota/`：额度优先级（首份 → 邀请 → 会员 → 付费）

### Phase 1 审查修复
- `savePet` openid 遗漏修复
- `api.js` openid 注入移除
- 知识库云函数返回格式修正（list→articles + hasMore）
- snake_case/camelCase 字段名兼容
- detail 页面字段名映射

---

## ✅ Phase 2：AI报告购买流程 — 模拟支付模式（已完成）

### Step 1: createOrder 云函数（模拟支付）
- `cloudfunctions/createOrder/`：MOCK_PAY 模式
- 额度解析 → 重复订单检查 → **先扣减额度再创建订单**（原子性保障）
- 新用户无记录时自动创建用户记录 + 标记 first_report_used
- 金额存储用分，out_trade_no 唯一

### Step 2: payCallback 云函数（骨架）
- `cloudfunctions/payCallback/`：TODO 注释标记真实支付实现步骤

### Step 3: 订单查询
- `cloudfunctions/orderList/`：分页查询，按时间倒序
- `cloudfunctions/orderDetail/`：单订单查询 + 归属验证

### Step 4: 风险结果页改造
- `pages/risk/result.js`：新增 checkReportQuota → getAIReport → createOrderAndReport → generateReport → viewExistingReport
- `pages/risk/result.wxml`：紫色渐变 AI 报告卡片 + 已购买绿色状态卡

### Step 5: AI 报告展示页
- `pages/ai-report/index.*`：8 章节折叠面板 + 风险徽章 + 分享 + 免责声明

### Step 6: 订单列表页
- `pages/order/list.*`：订单卡片 + 状态颜色 + 分页加载 + 预格式化显示字段

### Step 7: 页面注册 + 入口
- app.json 注册 ai-report/index, order/list
- "我的"页面 viewOrders 指向 /pages/order/list

### Phase 2 审查修复
- viewOrders 重复定义（旧版本"功能开发中"覆盖新版）→ 移除旧版本
- 新用户无记录 first_report_used 永不标记 → 自动创建用户记录
- 订单创建和额度扣减顺序 → 改为先扣减再创建
- 订单列表 WXML 内联三元过长 → 改用预计算字段

### Phase 3 审查修复
- memberActivate MOCK_PAY模式
- 新用户无记录自动创建
- 续费时间从旧过期日计算

### Phase 4 审查修复
- createInvite每日上限防刷
- 邀请码唯一性
- 排行榜缓存

### 商户号到位后切换清单
1. `cloudfunctions/createOrder/index.js` — `MOCK_PAY = false` + 添加 `cloud.cloudPay.unifiedOrder()`
2. `cloudfunctions/payCallback/index.js` — 填充签名验证逻辑
3. `miniprogram/pages/risk/result.js` — 添加 `wx.requestPayment()` 调用

---

## ✅ Phase 3：会员系统 + 回访闭环（已完成）

### 会员购买
- `cloudfunctions/memberActivate/`：激活/续费会员
- `pages/member/index.*`：会员中心首页
- `pages/member/order.*`：购买会员页
- `pages/member/status.*`：会员状态与权益
- 月卡 ¥19.90 / 年卡 ¥99.00
- createOrder 扩展支持 member 类型订单

### 会员权益
- 每月 3 次 AI 报告额度
- 无限自查
- 疫苗/驱虫提醒
- 历史报告永久保存
- 额度按月对齐到起始日重置

### 回访系统
- `cloudfunctions/followupSubmit/`：提交回访结果
- `cloudfunctions/getFollowupList/`：回访记录列表
- 首页回访弹窗：查询 pending 回访记录
- 订阅消息：`wx.requestSubscribeMessage` 24 小时提醒
- 回访数据用于优化规则引擎

---

## ✅ Phase 4：裂变增长 + 打磨（已完成）

### 邀请裂变
- `cloudfunctions/createInvite/`：创建邀请记录
- `cloudfunctions/getInviteStats/`：邀请统计
- `cloudfunctions/getInviteRanking/`：排行榜
- `cloudfunctions/generateSharePoster/`：服务端海报生成
- `pages/invite/index.*`：邀请页面
- `pages/invite/ranking.*`：排行榜页面
- 反作弊：设备指纹、IP 限流、最低使用门槛

### 分享功能
- 分享卡片：onShareAppMessage 定制
- 分享海报：服务端 canvas 渲染

### 首页改版
- 功能入口网格：症状自查 / AI报告 / 知识库 / 会员 / 邀请 / 回访记录
- 今日养宠知识卡片
- 会员专区 Banner

---

## 当前进度总览

| Phase | 内容 | 状态 | 新建文件 | 修改文件 |
|---|---|---|---|---|
| **Phase 1** | 安全修复 + 建库 + 知识库 + 报告引擎 | ✅ 已完成 | ~25 | ~30+ |
| **Phase 2** | 支付 + AI报告购买 | ✅ 已完成 | ~20 | ~5 |
| **Phase 3** | 会员系统 + 回访 | ✅ 已完成 | ~15 | ~5 |
| **Phase 4** | 裂变增长 + 打磨 | ✅ 已完成 | ~15 | ~5 |

---

## 关键数据

### 云函数总数
- V1.0: 15 个
- Phase 1 新增: 6 个（getKnowledgeList, getKnowledgeDetail, generateAIReport, getMemberStatus, checkReportQuota, dbInit 扩展）
- Phase 2 新增: 4 个（createOrder, payCallback, orderList, orderDetail）
- Phase 3 新增: 3 个（memberActivate, followupSubmit, getFollowupList）
- Phase 4 新增: 4 个（createInvite, getInviteStats, getInviteRanking, processInviteReward）
- **当前: ~32 个云函数**

### 页面总数
- V1.0: 9 个页面
- Phase 1 新增: 2 个（knowledge/list, knowledge/detail）
- Phase 2 新增: 2 个（ai-report/index, order/list）
- Phase 3 新增: 3 个（member/index, member/order, followup/submit）
- Phase 4 新增: 2 个（invite/index, invite/ranking）
- **当前: ~18 个页面**

### 数据库集合
- V1.0: 6 个（users, pets, symptom_records, ai_cache, orders, hospitals）
- Phase 1 新增: 5 个（members, knowledge_articles, invite_records, followup_records, report_templates）
- **当前: 11 个集合**

---

## V2.0 路线图

| 功能 | 优先级 | 依赖 |
|---|---|---|
| 图片识别（拍照识症状） | P0 | AI 模型训练 |
| 便便分析 | P1 | 图像识别基础 |
| 皮肤病识别 | P1 | 图像识别基础 |
| 企业微信 | P1 | 企业微信 API |
| 医院分佣 | P1 | 支付系统 + 合作协议 |
| 宠物商城 | P2 | 供应链搭建 |
| 管理后台 | P1 | 数据积累 |
